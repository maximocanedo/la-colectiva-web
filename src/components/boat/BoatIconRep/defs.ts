import {RegionType} from "../../../data/models/region";
import {IEnterprise} from "../../../data/models/enterprise";

export interface IBoatIconRepProps {
    name: string;
    mat: string;
    show?: boolean;
}